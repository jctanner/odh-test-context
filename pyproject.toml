[project]
name = "odh-tests-context"
version = "0.1.0"
description = "Per-repository test context extraction for AI-assisted patch validation"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.1.48",
    "python-dotenv>=1.1",
]
